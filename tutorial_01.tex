\documentclass[11pt]{article}

\begin{document}
 Namaste! This is my first \LaTeX\ document.\\
 %
 % Two types of return:
 % 1. Soft Return -> use two double slashes (\\) at the end of the line.
 % 2. Hard Return -> leave a blank line between and it will start a new paragraph.
 %
 A rectangle has side lenghts of $(x+1)$ and $(x+3)$.
 The equation $${A(x)=x^2+4x+3}$$ gives the area of the rectangle.
 
 %
 % Two types of math modes: 
 % 1. Displayed math mode ($$)
 % 2. Inline math mode ($)
 %
\end{document}
